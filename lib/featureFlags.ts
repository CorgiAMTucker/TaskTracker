/**
 * Broker Request intake is deprecated for now (board, data, and API
 * support all stay intact) — flip this back to true to bring it back:
 * the board tab, the Request Type option, and server-side acceptance
 * of BROKER_REQUEST submissions all key off this one flag.
 */
export const BROKER_REQUESTS_ENABLED = false;
